Frontend for Teacher's Day Greeting Generator

Run locally:

1. npm install
2. npm run dev

The frontend expects the backend to be proxied: Vite dev server should proxy `/api` to the backend during development.
